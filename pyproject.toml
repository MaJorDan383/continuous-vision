[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "continuous-vision"
version = "1.0.0"
description = "Continuous vision plugin for Hermes - monitors screens, windows, cameras, and routes to vision models"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
authors = [
    {name = "MaJorDan"}
]
keywords = ["vision", "camera", "monitor", "screen", "hermes"]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.9",
    "Operating System :: Microsoft :: Windows",
]
dependencies = [
    "opencv-python-headless>=4.5",
    "Pillow>=10.0",
    "openai>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
]

[project.urls]
Homepage = "https://github.com/MaJorDan383/continuous-vision"
Repository = "https://github.com/MaJorDan383/continuous-vision"
Issues = "https://github.com/MaJorDan383/continuous-vision/issues"

# This is a Hermes plugin: it is distributed and loaded as a DIRECTORY (plugin.yaml +
# __init__.py + dashboard/ + desktop/), not as an importable Python package — the directory
# name contains a hyphen, so it can never be imported by name. Declaring no packages keeps
# setuptools out of flat-layout auto-discovery (which otherwise tries to ship dashboard/ as a
# top-level package and fails); Hermes installs the `dependencies` above for its environment.
[tool.setuptools]
packages = []
